<h1>Editing pin</h1>

<%= render 'form' %>

<% if current_user.avatar.url.present? %>
    <%= image_tag(current_user.pins.url) %> 
    <%= f.label :remove_pins do %>
    <%= f.check_box :remove_pins %> 
  	<%= f.file_field :pins %>
<% end %>

<%= link_to 'Show', @pin %> |
<%= link_to 'Back', pins_path %>
